<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace = "com.sky.mapper.UserMapper">
	<select id="getNewUserCountByTimeRange" resultType="java.lang.Long">
		select count(id) from user where create_time between #{beginTime} and #{endTime}
	</select>
	<select id="getUserCount" resultType="map">
		select date(create_time) as date, count(id) as count from user where create_time between #{beginTime} and #{endTime} group by date(create_time)
	</select>
</mapper>